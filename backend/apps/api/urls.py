"""
Routes API v1 — les viewsets seront ajoutés au fur et à mesure
"""
from django.urls import path, include

urlpatterns = [
    # À compléter lors du développement des viewsets DRF
    # Exemple : path('sessions/', SessionViewSet.as_view({'get': 'list'})),
]
